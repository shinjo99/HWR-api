[phases.setup]
nixPkgs = ["python311", "pango", "harfbuzz", "cairo", "gdk-pixbuf", "libffi", "noto-fonts-cjk-sans"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
